apiVersion: v1
kind: Service
metadata:
  name: kubernetes-dashboard-nodeport
  namespace: kubernetes-dashboard
spec:
  selector:
    app: kubernetes-dashboard
  ports:
    - name: kubernetes-dashboard-nodeport
      protocol: TCP
      port: 443
      targetPort: 443
      nodePort: 30558
  type: NodePort