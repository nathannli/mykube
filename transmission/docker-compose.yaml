version: "3.8"

services:
  transmission:
    image: haugene/transmission-openvpn
    container_name: transmission-vpn
    cap_add:
      - NET_ADMIN
    env_file:
      - .env
    volumes:
      - /home/nathan/Downloads:/data
      - ./config:/config
    ports:
      - "9091:9091"
    restart: unless-stopped
