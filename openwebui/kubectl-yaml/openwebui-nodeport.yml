apiVersion: v1
kind: Service
metadata:
  name: openwebui-open-webui-nodeport
  namespace: openwebui
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30809
  selector:
    app.kubernetes.io/name: open-webui
