install docker desktop: https://docs.docker.com/desktop/setup/install/linux/debian/
- use the apt method, not the .deb method

automation functions are located in rpi5

created automation in HA, copy remote-start-computers from google home.
set trigger as webhook